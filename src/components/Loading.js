//import React
import React from "react";

//Loading Function displays loading info when results are loading
const Loading = () => (
    <li className="not-found">
        <p>are loading....</p>
    </li>
);
//Export Loading Function
export default Loading;